window.document.get = {};

document.location.search.replace(/\??(?:([^=]+)=([^&]*)&?)/g, function () {
    function decode(s) {
        return decodeURIComponent(s.split("+").join(" "));
    }

    window.document.get[decode(arguments[1])] = decode(arguments[2]);
});